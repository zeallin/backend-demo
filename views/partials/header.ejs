<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title><%= locals.title || "Express OpenID Connect" %></title>
  <%# comment %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@300;400&display=swap" rel="stylesheet">

  <!-- load tailwindcss -->
  <link rel="stylesheet" href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css">

  <!-- custom font css -->
  <style>
    body {
      font-family: 'Ubuntu', sans-serif;
    }
  </style>
</head>

<body class="bg-gray-200">
  <nav class="flex items-center justify-between flex-wrap bg-gray-800 p-6">

    <div class="flex items-center flex-shrink-0 text-white mr-6">
      <span class="font-semibold text-xl tracking-tight">Zeal Back-End Exam</span>
    </div>

    <div class="block flex-grow lg:flex lg:items-end lg:w-auto text-right">
      <div class="text-sm lg:flex-grow">
        <a href="/" class="mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Home</a>
        <% if (isAuthenticated === true) { %>
        <% if (locals.loginMgr.isEmailVerified(locals)) { %>
        <a href="/profile" class="mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Profile</a>
        <% } %>
        <a href="/logout" class="mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Logout</a>
        <% } else { %>
        <a href="/login" class="mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Login</a>
        <a href="/signup" class="mt-4 lg:inline-block lg:mt-0 text-gray-300 hover:text-white mr-4">Signup</a>
        <% } %>
      </div>

    </div>
  </nav>
  <div class="flex justify-center py-4">